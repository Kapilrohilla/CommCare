HTTP_PORT=3000
ENV=development

AWS_REGION=us-east-1
AWS_S3_BUCKET=my-bucket
AWS_ACCESS_KEY_ID=my-access-key-id
AWS_SECRET_ACCESS_KEY=my-secret-access-key

DEFAULT_STORAGE_VENDOR=s3

# Database
WRITER_DB_HOST=localhost
WRITER_DB_PORT=5432
WRITER_DB_USER=postgres
WRITER_DB_PASSWORD=postgres
WRITER_DB_NAME=postgres

READER_DB_HOST=localhost
READER_DB_PORT=5432
READER_DB_USER=postgres
READER_DB_PASSWORD=postgres
READER_DB_NAME=postgres

# Observability
METRICS_ENABLED=true
METRICS_PATH=/metrics
SERVICE_NAME=commcare
LOG_LEVEL=info
TRACING_ENABLED=true
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Redis (infra compose → localhost; app in Docker → redis)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_TLS=false

# Kafka (infra compose host access → localhost:9094; app in Docker → kafka:9092)
KAFKA_BROKERS=localhost:9094
KAFKA_CLIENT_ID=commcare
KAFKA_GROUP_ID=commcare-group
KAFKA_SUBSCRIBER=NONE
# Set ALL (or DEFAULT) on the API/worker process that runs BullMQ job handlers
BULLMQ_SUBSCRIBER=NONE
BULLMQ_SCHEDULER_ENABLED=false
BULLMQ_CONSUMERS_ENABLED=false
BULLMQ_WORKER_CONCURRENCY=5
BULLMQ_UI_ENABLED=true
BULLMQ_UI_PATH=/admin/queues

# Asterisk AMI (for asterisk-cdr-worker)
AMI_HOST=127.0.0.1
AMI_PORT=5038
AMI_USERNAME=your_ami_user
AMI_SECRET=your_ami_secret

# Auth
JWT_SECRET=dev-jwt-secret-change-in-production-min-32-chars

# Local dev only — Docker Compose overrides this for asterisk-cdr-worker
WEBHOOK_URL=http://localhost:3000/calls/cdr/webhook
WEBHOOK_TIMEOUT=10000
WEBHOOK_RETRIES=3

# Asterisk ARI
ARI_HOST=asterisk ari host (with port if have)
ARI_USER=asterisk ari username
ARI_PASSWORD=asterisk ari password
ARI_WS_URL=
# Set true only when running ARI consumer in-process (local dev). Docker uses commcare-ari-consumer service.
ARI_CONSUMER_ENABLED=false
ARI_LEADER_LOCK_TTL_SECONDS=10
ARI_LEADER_LOCK_RENEW_INTERVAL_MS=5000
# External click2call dial string — use {number} placeholder (FreePBX: Local/{number}@from-internal)
ARI_OUTBOUND_ENDPOINT_TEMPLATE=Local/{number}@from-internal

# FreePbx
FREEPBX_BASE_URL=freepbx base url
FREEPBX_GRAPHQL_URL=freepbx graphql url
FREEPBX_TOKEN_URL=freepbx token url
FREEPBX_CLIENT_ID=freepbx client id
FREEPBX_CLIENT_SECRET=freepbx client secret
FREEPBX_OAUTH_SCOPE=gql:core gql:framework

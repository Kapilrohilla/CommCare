[general]
enabled = yes
webenabled = no
port = 5038
bindaddr = 0.0.0.0
displayconnects = yes

; AMI user rendered at startup from env (see docker-entrypoint.sh)
#include "/var/lib/asterisk/generated/manager-user.conf"

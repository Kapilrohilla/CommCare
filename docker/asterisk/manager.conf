[general]
enabled = yes
webenabled = no
port = 5038
bindaddr = 0.0.0.0
displayconnects = yes

; Matches AMI_USERNAME / AMI_SECRET in .env (env-sample defaults).
[commcare]
secret = commcare-ami-secret
read = all
write = all
eventfilter = !Event: RTCPSent
eventfilter = !Event: RTCPReceived
